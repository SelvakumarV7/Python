# Tuple
    # Braces()
    # Allows Duplicate
    # Mutable(Diff. Datatypes allowed)
    # Slicing and indexing allowed
    # Modification or Editing not allowed
    # Casting allowed to convert into list and modify if need(casting: datatype change)
    # Functions:




a = (1,2,3,4,5,6,7,8,9)
b = a[0:5]
print(b)
print(a[0])