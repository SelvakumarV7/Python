# Encapsulation: as well as Encryption. it changes the variable as private variable and accessed only inside the same class using the function name only.
    # _   --> protected
    # __  --> Private

# Exception handling:
