# Set
    # Curly braces{}
    # Not Allows Duplicate, duplicate values are removed
    # Mutable(All datatype allows)
    # slicing and indexing not allowed
    # Modification and Editing not allowed but add and removal data is possible
    # Set is un-ordered
    #unctions: add(), update(), remove()